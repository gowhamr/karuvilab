export const radius = {
  sm: "var(--radius-sm)",
  default: "var(--radius-default)",
  lg: "var(--radius-lg)",
  xl: "var(--radius-xl)",
  "2xl": "var(--radius-2xl)",
  "3xl": "var(--radius-3xl)",
  /** @deprecated - Use 3xl (24px) or standard tokens instead. */
  container: "32px",
} as const;

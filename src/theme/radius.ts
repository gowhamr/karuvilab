export const radius = {
  sm: "var(--radius-sm)",
  default: "var(--radius-default)",
  lg: "var(--radius-lg)",
  xl: "var(--radius-xl)",
  "2xl": "var(--radius-2xl)",
  container: "32px",
} as const;
